# Evidence-Backed Dead/Unused Code Review

> Generated by systematic search across all `.py`, `.ps1`, `.bat`, `.yml`, `.md` files.
> Methodology for each claim: read the target file, identify exported symbols, search for every symbol and import path across the entire project.

---

## A. Dead Modules (5 files)

### A1. `backend/app/services/llm/base.py` (37 lines)

**Claim in audit:** "Unused abstract class — no implementation inherits from it"

**Exported symbols:** `class BaseLLM(ABC)` with abstract methods: `chat()`, `chat_complete()`, `structured_output()`, `is_available()`

**Incoming call graph:**
| Source | Line | Pattern | Found? |
|--------|------|---------|--------|
| (self) | 6 | `class BaseLLM` | Yes (definition) |
| Any `.py` file | — | `from app.services.llm.base import` | **NO** |
| Any `.py` file | — | `import app.services.llm.base` | **NO** |
| Any `.py` file | — | `class.*\(.*BaseLLM` | **NO** |
| Any `.py` file | — | string `"BaseLLM"` | **NO** |
| `llm/__init__.py` | — | `from .base import` | **NO** (only imports from `groq_service.py`) |
| `groq_service.py` | 1243 | `class GroqService:` | **NO** (standalone class, no parent) |

**Outgoing call graph:** Imports only stdlib (`abc`, `collections.abc`, `typing`). No project imports.

**Confidence: CERTAIN** — `BaseLLM` has zero consumers across the entire project. It is an orphaned abstract interface.

---

### A2. `backend/app/services/conversation/responses.py` (50 lines)

**Claim in audit:** "Unused templates — groq_service.py has its own responses"

**Exported symbols:** `RESPONSES` (dict), `class ResponseTemplates` with method `get(intent, language, default_lang)`

**Incoming call graph:**
| Source | Line | Pattern | Found? |
|--------|------|---------|--------|
| (self) | 45 | `class ResponseTemplates` | Yes |
| `conversation/__init__.py` | 3 | `from .responses import ResponseTemplates` | Yes (internal re-export) |
| Any production `.py` | — | `from app.services.conversation import ResponseTemplates` | **NO** |
| Any production `.py` | — | `from app.services.conversation.responses import` | **NO** |
| Any production `.py` | — | `import app.services.conversation.responses` | **NO** |
| Any production `.py` | — | string `"ResponseTemplates"` | **NO** |
| Any test file | — | string `"ResponseTemplates"` | **NO** |

**Outgoing call graph:** Imports only stdlib (`logging`, `typing.Optional`). `Optional` is actually unused in the file (minor lint issue).

**Evidence that `conversation/__init__.py` itself is never imported:**
- Searched: `from app.services.conversation import` — only found in test files:
  - `tests/test_session_isolation.py` imports `ConversationManager` (from `context`, not the package root)
  - `tests/test_intent.py` imports from `conversation.intent` directly
  - `tmp_verify_findings.py` imports `ConversationManager` from `conversation.context`
- **No production file** imports from `app.services.conversation`

**Confidence: CERTAIN** — The `__init__.py` re-exports `ResponseTemplates` but neither it nor any consumer ever imports from it.

---

### A3. `backend/app/services/query_logger.py` (85 lines)

**Claim in audit:** "Superseded by conversation_logger.py"

**Exported symbols:** `class QueryLogger` (singleton), `get_query_logger()` (factory)

**Incoming call graph:**
| Source | Line | Pattern | Found? |
|--------|------|---------|--------|
| (self) | — | `class QueryLogger`, `get_query_logger` | Yes |
| Any `.py` file | — | `from app.services.query_logger import` | **NO** |
| Any `.py` file | — | `import app.services.query_logger` | **NO** |
| Any `.py` file | — | string `"QueryLogger"` | **NO** |
| Any `.py` file | — | string `"query_logger"` | **NO** |

**Replacement evidence:** `app.utils.conversation_logger` exports `ConversationTelemetry` and `get_telemetry`, actively imported by:
- `groq_service.py:769` → `from app.utils.conversation_logger import get_telemetry`
- `livekit_agent.py:51` → `from app.utils.conversation_logger import get_telemetry`
- `simulate_conversations.py:39` (test script), `tests/test_telemetry.py:24`

**Outgoing call graph:** Imports stdlib + `app.config.settings`. No project module imports it.

**Confidence: CERTAIN** — Zero references to `QueryLogger` or `query_logger` anywhere in the project. The replacement (`conversation_logger`) is actively used.

---

### A4. `backend/app/services/telephony/base_adapter.py` (22 lines)

**Claim in audit:** "Unused interface — TwilioService doesn't extend it"

**Exported symbols:** `class TelephonyAdapter(ABC)` with abstract methods: `handle_incoming_call()`, `disconnect_call()`

**Incoming call graph:**
| Source | Line | Pattern | Found? |
|--------|------|---------|--------|
| (self) | 5 | `class TelephonyAdapter` | Yes |
| Any `.py` file | — | `from app.services.telephony.base_adapter import` | **NO** |
| Any `.py` file | — | `import app.services.telephony.base_adapter` | **NO** |
| Any `.py` file | — | `class.*\(.*TelephonyAdapter` | **NO** |
| Any `.py` file | — | string `"TelephonyAdapter"` | **NO** |
| `twilio_service.py` | — | `class TwilioService:` | **NO** (standalone, no parent) |
| `telephony/__init__.py` | — | Does not exist | **NO** (directory is not a package) |

**Outgoing call graph:** Imports only stdlib (`abc`, `typing`). No project imports.

**Confidence: CERTAIN** — Zero references to `TelephonyAdapter` anywhere in the project.

---

### A5. `backend/app/services/voice_session.py` (95 lines)

**Claim in audit:** "Dead code with broken import — imports non-existent stt_deepgram.py"

**Exported symbols:** `class VoiceSessionManager`, `get_voice_session_manager()` (singleton factory)

**Incoming call graph:**
| Source | Line | Pattern | Found? |
|--------|------|---------|--------|
| (self) | 13 | `class VoiceSessionManager` | Yes |
| (self) | 91 | `get_voice_session_manager` | Yes |
| Any production `.py` | — | `from app.services.voice_session import` | **NO** |
| Any production `.py` | — | `import app.services.voice_session` | **NO** |
| `tmp_verify_findings.py` | 100 | `import app.services.voice_session` | Yes (orphaned debug script, NOT production) |
| `tmp_verify_findings.py` | 98 | string `"voice_session"` | Yes (same script) |

**Broken import evidence:**
| Import Target | Exists? | Line in voice_session.py |
|--------------|---------|--------------------------|
| `from app.services.stt_deepgram import get_deepgram_service` | **NO** | Line 8 |
| `glob(**/stt_deepgram*)` | **NO** | — |
| `glob(**/deepgram*)` | **NO** | — |

**If `voice_session.py` is ever imported, it will raise `ModuleNotFoundError`.** No production code currently triggers this import.

**Confidence: CERTAIN** — Zero production imports, and the file is **unimportable** due to missing dependency.

---

## B. Dead/Orphaned Scripts (22 files)

### B1–B12: Standalone Test/Utility Scripts

All 12 files below have identical evidence patterns → **zero incoming references** from any production code (`backend/app/`, `scripts/livekit_agent.py`, `scripts/build.py`, or any `.ps1`/`.bat` launcher):

| # | File | Lines | Claim | Incoming Refs | Confidence |
|---|------|-------|-------|---------------|------------|
| 1 | `scripts/energy_vad.py` | 199 | "UNUSED — silero.VAD used instead" | **0** (only self-referencing import `from energy_vad import EnergyVAD`) | CERTAIN |
| 2 | `scripts/dispatch_agent.py` | 45 | "Superseded by join_room.py" | **0** | CERTAIN |
| 3 | `scripts/join_room.py` | — | "Manual utility" | **0** | CERTAIN |
| 4 | `scripts/validate_retrieval.py` | 345 | "Standalone QA tool" | **0** | CERTAIN |
| 5 | `scripts/performance_report.py` | 369 | "Standalone analysis" | **0** | CERTAIN |
| 6 | `scripts/simulate_conversations.py` | 992 | "Standalone test harness" | **0** | CERTAIN |
| 7 | `scripts/demo_readiness_test.py` | 402 | "Standalone test" | **0** | CERTAIN |
| 8 | `scripts/system_test.py` | 153 | "Standalone integration test" | **0** (note: it *imports* livekit_agent.py, not the reverse) | CERTAIN |
| 9 | `scripts/test_15_quick.py` | 99 | "Standalone ad-hoc test" | **0** | CERTAIN |
| 10 | `scripts/test_faq_comprehensive.py` | 260 | "Standalone test" | **0** | CERTAIN |
| 11 | `scripts/test_rag_150.py` | 406 | "Standalone test runner" | **0** | CERTAIN |
| 12 | `scripts/test_vp_fix.py` | 34 | "Standalone debug script" | **0** | CERTAIN |

**Methodology for each:** Searched for bare filename (e.g. `energy_vad`) across all `.py` files, plus all `.ps1`/`.bat`/`.yml` files. Also searched for `subprocess.run(Popen/call)` arguments containing the filename. No matches outside the file's own code found for any of the 12.

---

### B13–B19: Overlapping/Superseded Launchers

| # | File | Claim | Incoming Refs | Confidence |
|---|------|-------|---------------|------------|
| 13 | `scripts/start_backend.ps1` | "Overlaps with root start_server.ps1" | **0** | CERTAIN |
| 14 | `scripts/start_backend.bat` | "Overlaps with root start_server.ps1" | **0** | CERTAIN |
| 15 | `scripts/start_agent.bat` | "Overlaps with root start_agent_now.ps1" | **0** | CERTAIN |
| 16 | `scripts/server_bg.py` | "Superseded by start_server.ps1" | **0** | CERTAIN |
| 17 | `scripts/start_server.py` | "Superseded by start_server.ps1" | **0** | CERTAIN |
| 18 | `scripts/run_agent.py` | "Superseded by start_agent.py → start_agent_now.ps1" | **0** | CERTAIN |
| 19 | `scripts/start_agent.py` | "Superseded by start_agent_now.ps1" | **0** | CERTAIN |

**Evidence:** The root-level `start_server.ps1` and `start_agent_now.ps1` serve as canonical launchers for the actual production services. None of the 7 scripts/ launchers are referenced by any production code or root launcher.

---

### B20–B24: One-Time Data Collection Scripts

| # | File | Claim | Incoming Refs | Confidence |
|---|------|-------|---------------|------------|
| 20 | `scripts/download_pdfs.py` | "One-time data collection" | **0** | CERTAIN |
| 21 | `scripts/parse_all_pdfs.py` | "One-time data collection" | **0** | CERTAIN |
| 22 | `scripts/parse_fee_pdfs.py` | "One-time data collection" | **0** | CERTAIN |
| 23 | `scripts/scrape_official_site.py` | "One-time data collection" | **0** | CERTAIN |
| 24 | `scripts/update_kb_from_pdfs.py` | "Manual data update" | **0** | CERTAIN |

**Evidence:** None of these are referenced by `build.py` (which uses `validate_kb.py`, `generate_kb.py`, `generate_markdown.py`, `generate_faq.py`, `generate_embeddings.py` — a completely different set). No production code references them.

---

## C. Backup Service — NOT Dead but IS a Placeholder

**File:** `backend/app/services/backup.py`

**Re-evaluation:** The audit marked this as "dead code," but that label is **incorrect**.

**Incoming call graph:**
| Source | Line | Pattern |
|--------|------|---------|
| `admin.py` | 20 | `from app.services.backup import BackupService` |
| `admin.py` | 28 | `backup_service = BackupService()` |
| `admin.py` | 160 | `backup_service.create_backup()` — route `POST /admin/backup/create` |
| `admin.py` | 170 | `backup_service.list_backups()` — route `GET /admin/backups` |
| `admin.py` | 178 | `backup_service.delete_backup(filename)` — route `DELETE /admin/backup/{name}` |

**Revised classification:** **ACTIVE CODE**, not dead. However, the implementation is a **placeholder/stub** — `create_backup()` writes a dummy `dummy_backup.txt` file, not a real backup. The claim in the audit should have said "placeholder implementation" not "dead."

**Confidence: CERTAIN** — BackupService IS wired to 3 admin routes.

---

## D. GEMINI_API_KEY — Dead Configuration

**Claim:** "GEMINI_API_KEY defined but never used"

| File | Line | Content |
|------|------|---------|
| `backend/.env` | 4 | `GEMINI_API_KEY=AIzaSyBFYi_pDma2xSlBXFZWkXaIM_OV09MIb_8` |
| `backend/app/config.py` | 31 | `gemini_api_key: str = ""` |

**Search for usage of `gemini_api_key` across all `.py` files:**
- `settings.gemini_api_key` — **0 matches**
- `config.gemini_api_key` — **0 matches**
- `google.generativeai` — **0 matches**
- `GoogleGenerativeAI` — **0 matches**
- `genai` — **0 matches**
- Any reference to `Gemini` in `backend/app/` directory — only `config.py` line 31

**The only references to Gemini anywhere:**
- `backend/.env:4` — key definition
- `backend/app/config.py:31` — field declaration
- `backend/benchmarks/test_async_blocking.py:35` — `self.gemini_client = None` (mock skip)
- `backend/tests/test_async_groq.py:76` — `svc.gemini_client = None` (mock attribute)
- `tmp_verify_findings.py:133` — comment string `"Flow: Gemini + Groq both fail"`

**Confidence: CERTAIN** — The key is populated in `.env` and loaded into `settings.gemini_api_key`, but **no code ever reads it or uses it**.

---

## E. `conversation/intent.py` — Dead Code

**File:** `backend/app/services/conversation/intent.py` (409 lines)

**Exported symbols:** `Intent` (Enum), `IntentClassifier` (class with `classify()` method)

**Incoming call graph:**
| Source | Line | Pattern |
|--------|------|---------|
| `conversation/__init__.py` | 2 | `from .intent import IntentClassifier, Intent` (internal re-export) |
| `tests/test_intent.py` | 2 | `from app.services.conversation.intent import ...` (test only) |
| Any production `.py` | — | `from app.services.conversation.intent import` | **NO** |
| Any production `.py` | — | `from app.services.conversation import IntentClassifier` | **NO** |

**Additionally:** `llm/intent_classifier.py` (510 lines, embedding-based) also has **zero production imports**.

**Comparison with `groq_service.py`:**
- `groq_service.py` does NOT contain a `_classify_intent` method
- It has its own inline detection methods: `_detect_structured_intent`, `_detect_repeat_intent`, `_detect_placement_eligibility_intent`, `_detect_repeat_conversation_intent`
- These are entirely separate from both `conversation/intent.py` and `llm/intent_classifier.py`

**Confidence: CERTAIN** — Both intent classifiers are orphaned. `groq_service.py` uses its own inline detection patterns.

---

## F. Root Orphaned Files

### `test_*.json` files (6 files)
| File | In Production Code? | In Any Code? |
|------|-------------------|--------------|
| `test_comprehensive.json` | **NO** | **NO** |
| `test_addr.json` | **NO** | **NO** |
| `test_principal.json` | **NO** | **NO** |
| `test_output.json` | **NO** | **NO** |
| `test_groq.json` | **NO** | **NO** |
| `test_full.json` | **NO** | **NO** |

**Note:** `tests/test_analytics.py` has method names `test_full_coverage` and `test_output_length` — these are test method names, not references to `test_full.json` or `test_output.json`.

**Confidence: CERTAIN** — No code reads these JSON files.

### `kb_comparison.txt` and `kb_structure.txt`
- Searched across all `.py`, `.ps1`, `.bat`, `.md` files → only referenced in `audit/DEAD_CODE.md` (our own audit document).
- **Confidence: CERTAIN** — Documentation artifacts.

### `tmp_verify_findings.py`
- Searched across all `.py` files → only self-references.
- Imported by no production code.
- **Incoming callers:** Zero.
- **Confidence: CERTAIN** — Ad-hoc verification script.

### `nul` file (empty 0-byte file in root)
- Git refuses to track it (`fatal: invalid path 'nul'` — Windows reserved device name).
- Blocks `git add -A`.
- **Confidence: CERTAIN** — Accidental artifact, must be excluded or removed.

---

## G. Updated Status After Evidence Review

| Claim | Original Label | Correct Label | Confidence |
|-------|---------------|---------------|------------|
| `llm/base.py` | Dead | **Dead** (orphaned ABC) | CERTAIN |
| `conversation/responses.py` | Dead | **Dead** (never imported) | CERTAIN |
| `query_logger.py` | Dead | **Dead** (superseded by `conversation_logger`) | CERTAIN |
| `telephony/base_adapter.py` | Dead | **Dead** (orphaned ABC) | CERTAIN |
| `voice_session.py` | Dead | **Dead** + **unimportable** (broken import) | CERTAIN |
| `backup.py` | Dead | **CORRECTION: Active but stub** (wired to 3 admin routes, but produces dummy files) | CERTAIN |
| 22 scripts | Dead | **Dead** (zero incoming references from production code) | CERTAIN |
| GEMINI_API_KEY | Unused | **Unused** (defined, loaded, never consumed) | CERTAIN |
| `conversation/intent.py` | Superseded | **Dead** (zero production imports; `groq_service.py` uses its own inline patterns) | CERTAIN |
| `llm/intent_classifier.py` | Superseded | **Dead** (zero production imports) | CERTAIN |
| Root test JSONs | Orphaned | **Orphaned** (zero code references) | CERTAIN |
| `kb_comparison.txt` | Artifact | **Orphaned** | CERTAIN |
| `tmp_verify_findings.py` | Orphaned | **Orphaned** (zero incoming callers) | CERTAIN |
| `nul` file | Problematic | **Problematic** (blocks `git add -A`) | CERTAIN |
| `conversation/__init__.py` package | — | **Dead package** (whole `conversation/` directory has zero production imports) | CERTAIN |
